# to-do list for jarvis

- [x] add a subcommand for `sudo shutdown now` In in `jarvis` command
- [ ] add a subcommand for `❯ tree --gitignore --dirsfirst` like `jarvis tree`
- [ ] add a new command for the `fresh-ubuntu-setup`
