# to-do list for jarvis

- [x] add a subcommand for Bkash which will do `SendMoney && Cashout`, `SendMoney X`, `Cashout from X` and `Cashout for X`
- [x] add a short url of the long raw github link
- [ ] add a command for cloning the repo of this project
- [ ] add a `poweroff` command with optional timing in min(s). Like: `jarvis poweroff 5`
- [ ] add a new command for the `fresh-ubuntu-setup`
